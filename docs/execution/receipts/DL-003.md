# DL-003 verification receipt

Date: 2026-07-12
Status: implementation complete and sealed; guarded landing, post-merge
verification, exact push/re-fetch, and feature-worktree cleanup pending

## Identity

- Base: `bbce281c4022a9389ff42a9c992b37d0630f7303`
- Branch: `codex/dl-003-recovery-gate`
- Worktree: `E:\Claude Code Projects\Personal\PC-SDK-Next-dl-003`
- Contract commit: `3025b50e142fca1c36d5dda1636a4fe6c450e89e`
- Sealed implementation: `eef661d7e97e6a9ce66568afff88a081129896aa`
- Sealed implementation tree: `d0351abbd2349c1ca739bc1ed16c804667943b65`
- Feature record: pending this documentation commit
- Landing merge: pending
- Exact feature/merge tree: pending
- Requirements: `OPS-003` verified; `WT-004`, `WT-005`, and `WT-006`
  strengthened and remain `accepted`

## Delivered invariant

A landed receipt proves merge history, never cleanup. Fresh landing and boot
re-drive use one positive settlement door. The project's immutable repository
identity must match the producer receipt, the worktree must be the exact
project/contract/run/path/name/branch/base snapshot, and present-path cleanup
or removal must revalidate exact Git registration and repository authority.
Only positive directory absence, successful exact registration absence,
expected-tip deletion of the exact merged branch, successful exact ref absence,
and exact row compare-and-set allow producer lifecycle `completed`.

Active and stranded rows retry. An exact already-destroyed row feeds only the
crash window between row settlement and lifecycle completion, with all
external absence re-proved. Locked, drifted, ambiguous, missing-identity,
unreadable, or CAS-lost evidence remains stranded/retryable. Stranded
reconciliation may settle only receipt-backed DL-002 abandonment and cannot
infer landed cleanup from a missing directory.

Terminal runs remain in the recovery read while they are the exact current
owner of an active/stranded worktree; ownership transfer or positive landing/
abandonment removes them. The Activity rail composes the existing run,
contract, and worktree read models into one strongest-evidence card, separately
counts running and recovery state, and exposes only Inspect plus DL-002's
current-eligibility Abandon action. Loading/failed reads do not claim current
preservation or empty success. HTTP omission is ordered by a server-authored
outbox high-water and resource cursors, not timestamps or browser clocks.

Production and the provider-free hard-kill fixture share one ordered pre-
attach repository recovery function. After a real process kill, the sealed
peer re-verifies, merges once, proves ancestry, and tears down positively. The
unsealed peer fails once with `server-restart` while exact branch, directory,
content, run, contract, and stranded evidence survive. A second boot changes
no merge, terminal, stranding, cleanup, contract, or outbox receipt.

## Verification evidence

- Persistence tests cover exact unresolved-owner retention, ownership
  transfer, positive settlement removal, and active/stranded/destroyed cleanup
  feeders.
- Real-Git landing, stranding, and mechanics tests cover locked registration,
  ref-lock retry, registration drift, missing project identity, full-snapshot
  row settlement, generic teardown registration proof, and landed missing-path
  non-resolution.
- Browser tests cover recovery projection/deduplication, cleanup crash states,
  stale terminal and running resource omission at the server high-water,
  explicit unavailable reads, conservative preservation claims, transcript
  evidence, separate counts, and fail-closed action eligibility.
- The real OS hard-kill test creates two recorded isolated worktrees, kills the
  owning process, executes production's shared recovery composition twice, and
  proves exact outcomes and positive temp/process cleanup.
- Final exact implementation-tree `pnpm ci:check` passes all workspace
  typechecks/tests, 419/419 server tests, and the dead-import guard.
- Production `pnpm --filter @pc-sdk/web build`, `git diff --check`, actual-path/
  allowed-path inventory, sealed commit/tree, and contract ancestry pass.

No live provider call, PM/MCP-network call, stable data access, original
PC-SDK mutation, or external repository mutation was required.

## Hostile review

Independent teardown/persistence, recovery/process, and browser/projection
lanes found and drove fixes for:

- retrying stranded landed rows and requiring positive registration plus exact
  branch absence before settlement;
- project identity binding, full-snapshot row CAS, present-path revalidation,
  and fail-closed ownership drift at destructive doors;
- making the OS-kill gate execute production-owned recovery composition;
- retaining cleanup crash lifecycles and landed merge-ready crash windows;
- preventing stale live terminal or running resources from resurrecting after
  authoritative HTTP omission without cross-clock inference;
- refusing stale worktree preservation claims during loading/failure and
  refusing Abandon when current contract eligibility evidence is absent; and
- making run/worktree read failure visible without projecting zero as success.

All three final read-only re-reviews report no remaining P0/P1/P2 blocker.

## Deliberate boundary

This slice adds no recovery database, general recovery center, workflow engine,
arbitrary Git repair, browser continuation command, or new destructive
authority. `WT-004`, `WT-005`, and `WT-006` remain `accepted` rather than
globally verified because nonparticipant/manual Git tools, escaped children,
and exact detached-review checkout authority remain separate. `OPS-006`,
PM/MCP, Codex, local-secret injection, N7, and stable-app changes remain out of
scope.

## Landing and cleanup receipt

Pending guarded landing of the feature record. Closeout must record exact
feature-record and merge commits, exact feature/merge tree equality, positive
ancestry, post-merge 419/419 gate, exact push/re-fetch, and guarded feature-
worktree/residue removal before this status becomes complete.
