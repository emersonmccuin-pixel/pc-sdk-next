# BC-002 completion receipt

Outcome: feature gate passed; landing and feature-worktree teardown pending

- Contract/slice: `docs/execution/slices/BC-002.md`
- Starting base SHA:
  `36ac71c59bb1d4095e30c9e2e4ed4d8ef73c9fd1`
- Preserved evidence SHA:
  `e233aa54c58dca163e98cf6011e79a0b91bd2d6f`
- Feature branch: `codex/bc-002-browser-baseline`
- Feature worktree:
  `E:\Claude Code Projects\Personal\PC-SDK-Next-bc-002`
- Slice-definition commit:
  `da92488985c0f956496c320d144d38b0268ff487`
- Sealed commit SHA: pending
- Target branch: `main`
- Target before/after SHA: pending / pending
- Positive ancestry/tree result: pending

## Evidence result

The direct browser matrix in `docs/research/browser-baseline.md` establishes an
objective N1 characterization pass, subject to the closing gates below.
Preserved/current desktop and narrow screenshots are tracked with exact SHA-256
receipts. The seven audited shell source blobs are identical between the fixed
subjects, and desktop/narrow geometry matched without document-level horizontal
overflow or browser console warnings/errors.

The current journey directly covered project/session isolation; ordered typed
chat/tool/control/activity/context projections; FIFO queue edit/remove;
interrupt confirmation before replacement delivery; historical/read-only and
resumed provenance; exact and unavailable context; provider-neutral quota;
run/contract/worktree states; safe agent transcript provenance; reload
equivalence; and one-message/one-projection two-tab convergence. No rendered
private reasoning, raw native session ID, or raw continuation-attempt ID was
observed.

Recorded non-N1 follow-up is honest rather than promoted into a pass: both
subjects inherit a roughly `101.197px` composer at `480px`, Escape does not
dismiss App Settings in either subject, and both fixture listeners bound to
wildcard IPv6 `::`. The first two remain N7 polish; loopback-only exposure and
`OPS-006` remain N7 unless explicitly reordered, while data/repository
single-owner admission is N4 work.

## Actual changed paths

The final exact 14-path audit found these BC-002-owned paths and no others:

- `apps/server/test/browser-baseline-fixture.ts`
- `docs/research/browser-baseline-preserved-fixture.ts`
- `docs/research/browser-baseline.md`
- `docs/research/browser-baseline-assets/current-desktop.png`
- `docs/research/browser-baseline-assets/current-narrow.png`
- `docs/research/browser-baseline-assets/preserved-desktop.png`
- `docs/research/browser-baseline-assets/preserved-narrow.png`
- `docs/research/baseline-characterization.md`
- `docs/current-state.md`
- `docs/master-plan.md`
- `docs/requirements.md`
- `docs/execution/current.md`
- `docs/execution/slices/BC-002.md`
- `docs/execution/receipts/BC-002.md`

The slice now names the actual checked test-fixture path. Any deviation from the
final declared path set requires an explicit scope note in the slice.

Scope note: hostile review found that deleting the disposable historical
worktree also deleted the only preserved-fixture source, contradicting the
slice's reproducibility objective. The inert tracked artifact
`docs/research/browser-baseline-preserved-fixture.ts` was therefore added and
declared explicitly. It is copied into a detached `e233aa54` worktree for its
historical typecheck/run; it is never compiled or imported by current
production composition.

## Verification commands and results

Verification host: Windows `10.0.26200.0`, PowerShell `7.5.8`, Node
`v22.13.0`, pnpm `10.33.0`; the in-app browser's exact embedded engine version
was unavailable from the captured receipt. Exact reproducibility commands and
assertions are recorded in `docs/research/browser-baseline.md` under
“Reproduction procedure.” The command sequence was:

- current: `pnpm install --offline --frozen-lockfile`, `pnpm typecheck`,
  `pnpm --filter @pc-sdk/web build`, then
  `$env:PC_BROWSER_PORT='55824'; pnpm --filter @pc-sdk/server exec tsx
  test/browser-baseline-fixture.ts`; `/health` reported `bc-002-fixture`;
- preserved: `git worktree add --detach <check> e233aa54...`, copy tracked
  `docs/research/browser-baseline-preserved-fixture.ts` byte-for-byte to
  `<check>/apps/server/test/browser-baseline-fixture.ts`, then the same offline
  install/typecheck/build against `<check>`;
- preserved start: historical `node <tsx-cli> <historical-fixture>` with
  `PC_BROWSER_BASELINE_ROOT=<new temp child>`, port `55823`, auto-exit `12000`,
  Anthropic canary values, and temp `CLAUDE_CONFIG_DIR`/`CODEX_HOME` sentinels;
  `/health`, `/api/projects`, and `/api/accounts` were asserted before positive
  exit/root-removal/port-closure checks.

The current fixture hash is
`00cfc83d47360975b29bcbcaff6075b464dfd77c2bdd8285e93b92019741ee48`;
the byte-identical preserved artifact/copy hash is
`3525029e7b3d97f0e05275bf070b37448147b7f7c12582ed2210621fdcc102cd`.

Passed on the final pre-seal tree:

- locked offline current install: all `471` packages reused, zero downloads;
- current workspace typecheck;
- current production web build;
- preserved detached-worktree workspace typecheck;
- preserved production web build;
- preserved tracked-artifact startup/HTTP/account/project/auto-cleanup retest;
- final current fixture server typecheck;
- final current fixture auto-exit startup/cleanup self-check;
- direct preserved/current browser matrix at `1440x900` and `760x720`, with
  `480x720` stress checks;
- exact screenshot, bundle-asset, and shell-blob hash collection.
- final 14-path scope audit and 139-reference local-path audit with no missing or
  unexpected result;
- final `git diff --check`;
- final feature-tree `pnpm ci:check` after hostile-review corrections,
  including all workspace typechecks/tests,
  `331/331` server tests, and the dead-import guard.

Pending before this receipt may say `Outcome: passed`:

- clean feature seal;
- guarded merge, positive ancestry and feature/landing tree equality;
- post-merge `pnpm ci:check`;
- push to `origin/main`.

The browser run used a deterministic fake runtime and disposable local state.
It made no live Claude/Codex turn, provider quota acquisition, PM call, MCP
call, launcher installation, stable-data access, or external-repository
mutation. Process/socket inspection found fixture listeners and loopback browser
connections only at observation time. It was not packet-level or complete
host-network instrumentation and is not claimed as such.

## Review and cleanup

- Review authorizer and decision: the first independent hostile review found a
  matrix-schema/reproducibility P1 pair and a cleanup-command P2. The tracked
  preserved artifact, exact R1-R12 matrix/guard classifications, named backlog
  evidence, and executable guarded teardown resolved them. Two independent
  final read-only re-reviews reported no remaining P0/P1/P2 blocker; root
  accepted the corrected feature gate.
- Earlier current fixture processes `53308` and `141708` were positively
  stopped and their disposable data removed.
- The corrected final account-menu reload showed only the two credential homes
  beneath `pc-sdk-test-mo1ykJ\fixture-credentials`; both current-tab console
  collections remained empty. The viewport override was reset and every test
  tab was closed.
- Exact command-line checks preceded termination of preserved PID `51452` and
  current PID `190104`. Ports `55823` and `55824` then had no listener.
- Every deleted fixture path was first resolved beneath the OS temp root. This
  removed all `pc-sdk-next-bc002-*` wrappers and
  `C:\Users\emers\AppData\Local\Temp\pc-sdk-test-mo1ykJ`.
- The detached preserved worktree showed no tracked or staged diff. Git
  deregistered it; its remaining exact unregistered dependency residue was
  separately path-checked and removed. The stable checkout retained its exact
  pre-run HEAD and clean status.
- Only the BC-002 feature-worktree teardown remains pending positive landing
  proof.

## Known limitations and follow-up

BC-002 changes no product behavior and does not claim live provider, PM, MCP,
external Git, shortcut, notification, or daily-driver migration coverage. It
does not substitute direct browser observation for the durable invariant tests
landed in CF-001 through CF-004 and RS-001 through RS-004. Remaining N4 work
includes process/data ownership, a cross-process repository lease,
child-environment isolation, and residual readiness/recovery receipts. Codex
adapter parity remains N5; loopback binding, the very-narrow composer, and
modal-keyboard polish remain N7 unless an explicit safety slice reorders the
listener fix.
