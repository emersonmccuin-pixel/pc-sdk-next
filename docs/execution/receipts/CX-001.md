# CX-001 verification receipt

Date: 2026-07-13
Status: complete in code — sealed, guarded-landed, post-merge verified, pushed,
and feature worktree removed; documentation closeout pending

## Identity

- Base: `dea2df76ff623ec96123b61ca6b9ab5f8aa8d639`
- Branch: `codex/cx-001-codex-subscription-spike`
- Worktree: `E:\Claude Code Projects\Personal\PC-SDK-Next-cx-001` (removed)
- Contract commits: `02135481c579109a7a5014e004525450562e975a`,
  `27d5ea5482076dd26509f789c0d5079b65c0eb6f`, and
  `8c15023e95bd3dfd1143d37c924fb4022f6e4c30`
- Sealed implementation: `648b7d971c34ccf36985d84c0d20155e5eacf7d3`
- Sealed implementation tree: `2e10894429d4a99cce91b3665b45585240c52bde`
- Feature record: `b9fce8df104570d383c392c506f67200ba001336`
- Code landing merge: `11365f1cf5802075d55b6506ebf5785a4e1ded5c`
- Exact feature/code-merge tree: `14bd4f9c93ab481dbd5c2443a76c1d1fb7a556ec`
- Feature branch: preserved at the exact feature record
- Feature worktree: deregistered; exact residual directory removed after
  parent/name/deregistration/no-`.git`/ancestry/remote/zero-process guards
- Documentation closeout branch/worktree: `codex/cx-001-closeout` /
  `E:\Claude Code Projects\Personal\PC-SDK-Next-cx-001-closeout`
- Requirements: none promoted. CX-001 supplies bounded evidence for
  `ARCH-003` through `ARCH-005`, `RUN-002`, `RUN-007`, `SEC-001`, and
  `SEC-003`; `RUN-001`, `RUN-003`, and `RUN-004` remain observational only;
  Codex `CTX-002`, `CTX-003`, `USE-002`, and `USE-003` remain unavailable.

## Delivered invariant

The repository pins and directly invokes native `@openai/codex@0.144.1`, with
no PATH, wrapper, shell, experimental protocol, or API-billed fallback. Its
598-file stable generated TypeScript schema has exact inventory/tree hashes and
a checkout-stable LF policy. Provider-native vocabulary is confined to the
Codex runner, its thin script, tests, and evidence docs.

The admission client starts from the shared positive child environment, adds
only one exact existing `CODEX_HOME`, and forces the file credential store at
native startup. Stable config admission requires an active exact-version
`sessionFlags` provenance layer and built-in OpenAI routing before cached
`account/read { refreshToken: false }` ChatGPT evidence or paginated visible
catalog discovery can proceed.

The client admits only initialize, strict remote-control-disabled evidence,
config/read, account/read, and model/list. It sends no thread, turn, login,
logout, tool, MCP, approval, context, quota, or inference method. Requests are
strictly correlated and normalized once; server requests, warnings, stderr,
unexpected notifications, malformed frames, and ambiguous evidence fail
closed with redacted codes.

The first directly owned native child must have matching exit/close receipts,
drained streams, and a finished decoder before a distinct second process can
repeat the admission. Success requires exact advertised default model/effort
agreement, the same positive second-process teardown, and verified disposable
workspace removal.

## Verification evidence

- Exact package/native version: `@openai/codex@0.144.1` / `codex-cli 0.144.1`.
- Generated stable schema: 598 files; inventory hash
  `sha256:96edfa58afbd0925a74a4e7575df581c05525567965b44d61cdf2fc43d8eb5f8`;
  tree hash
  `sha256:bd4ea5dcabc98ffb47f566a2c7f5c44b896b7d681372769fbcfe22216bc34faa`.
- Complete focused Codex/schema/environment/static suite: 153/153 passed,
  including exact native schema regeneration.
- Provider-free native empty-home gate: strict initialize/config admission,
  expected redacted `account-admission-failed`, positive cleanup, and zero
  repository-owned Codex processes.
- One explicitly approved no-turn live gate: exact package 0.144.1; exact-home
  file-store cached ChatGPT auth kind; built-in routing; advertised default
  `gpt-5.6-sol` with supported default effort `medium`; exact agreement across
  two distinct directly disposed native processes; temporary root removed;
  zero repository-owned Codex processes before and after.
- Full feature-tree `pnpm ci:check`: all workspace typechecks/tests, 597/597
  server tests, and dead-import guard passed.
- Production `pnpm --dir apps/web build`: passed.
- `git diff --check`, exact 617-path allowed-scope audit, generated LF
  attributes, and changed-path credential-home literal audit passed.
- Three independent final hostile re-reviews report no remaining P0/P1/P2
  finding.

No PM/MCP network call, stable-app data access, original-PC-SDK mutation, model
turn, or external-repository mutation was performed.

## Hostile review

Independent process/protocol, admission/security, and contract/static reviews
drove fixes for reentrant initialization disposal; request-parameter accessor
TOCTOU; cancelled-response and EPIPE shutdown races; cleanup uncertainty and
failed-spawn lifecycle precedence; exact active matching-version file-store
provenance; config false positives; silent canonical Windows invocation;
generated-schema EOL reproducibility; and static inclusion of the live script.

Final focused suites, typecheck, schema regeneration, diff hygiene, and all
three narrow re-reviews are clean.

## Deliberate boundary

Cached auth and catalog presence do not establish credential freshness,
entitlement, subscription usability, billing route, model usability, or
inference. CX-001 provides no production adapter, registration, runtime/session
stamp, create/resume/continuation, terminal, interruption, approval, tool/MCP,
context/quota, canonical mapping, dispatch, handoff, selector, or real-fix
evidence. It does not claim byte-level credential-home immutability or retain
raw frames, identity, plan, native IDs, config/origins, quota, provider prose,
tokens, or reasoning.

Direct-child lifecycle proof is positive. Mechanically enforced containment or
absence proof for escaped/reparented descendants remains unavailable and blocks
a production adapter. No requirement status changes.

## Landing and cleanup receipt

Implementation `648b7d971c34ccf36985d84c0d20155e5eacf7d3` and feature
record `b9fce8df104570d383c392c506f67200ba001336` guarded-landed as code
merge `11365f1cf5802075d55b6506ebf5785a4e1ded5c`. Contract,
implementation, and feature-record ancestry are positive. Feature and code
merge resolve to exact tree `14bd4f9c93ab481dbd5c2443a76c1d1fb7a556ec`.

Post-merge `pnpm ci:check` passed all workspace typechecks/tests, 597/597
server tests, and the dead-import guard. Production web build and focused
schema/static checks passed. The merge was pushed and re-fetched exactly as
`origin/main` before cleanup.

The clean feature worktree was deregistered only after exact landing, tree,
push, feature-tip, path, and process proof. Git left an exact residual directory;
its parent/name, deregistration, no-`.git`, preserved-branch ancestry, exact
remote, known-name, and zero-process guards passed before recursive removal.
The feature branch remains at the exact feature record.

After documentation closeout, the next bounded action is to define CX-002 for
a provider-local `CodexRuntimeAdapter` behind `AgentRuntimeAdapter` and shared
provider-free conformance. Production registration, selectors, handoff UI,
live turns, and the real-fix gate remain blocked until pre-execution tool/MCP/
approval enforcement and escaped-descendant containment have positive receipts.
Experimental protocol and API-billing fallback remain unauthorized.
