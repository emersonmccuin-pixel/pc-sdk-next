# CX-002 verification receipt

Date: 2026-07-13
Status: complete — implementation and documentation guarded-landed, post-merge
verified, pushed/re-fetched exactly, and all CX-002 worktrees removed

## Identity

- Base: `da1376c334c78c9e485df7fe2d3a6d3b6af05c17`
- Branch: `codex/cx-002-codex-runtime-adapter`
- Worktree: `E:\Claude Code Projects\Personal\PC-SDK-Next-cx-002` (removed)
- Contract commit: `23b2b90ca75b47daa7fca1ccf9f859e2b7c271ef`
- Sealed implementation: `bf1f3a5ec8a12c17defa954c1bd5ccf3c59f4e87`
- Sealed implementation tree: `9de0007e4420849af4cf4b7f999856167f29c46f`
- Feature record: `98ce745db5e440a2f5c45fe2e620c00fda427dbf`
- Code landing merge: `611c304eaa8932900b4f9d339edbb2058d99fa0f`
- Exact feature/code-merge tree:
  `ce1c31e8ba38095a6e7571f2fe2705939436e645`
- Feature branch: preserved at the exact feature record
- Feature worktree: deregistered; the exact residual directory was removed only
  after parent/name, no-`.git`, ancestry, remote, branch-tip, and zero-process
  guards
- Documentation closeout branch: `codex/cx-002-closeout` (preserved)
- Documentation closeout commit:
  `30fe8bb5604d68eb924f73bf2b40d0f4d4a71e3c`
- Documentation closeout merge:
  `a48db361026fc999bd4226797f329a85c0e795d7`
- Documentation closeout tree:
  `2bb0d86efee8316711985edb88a1022b6ad52cad`
- Documentation closeout worktree: removed after exact path, clean tip,
  documentation-only scope, ancestry, remote, and zero-process guards
- Requirements: none promoted. CX-002 supplies bounded implementation evidence
  for `ARCH-003` through `ARCH-005`, `RUN-002`, `RUN-004`, `RUN-007`,
  `SEC-001`, and `SEC-003`; all global statuses remain unchanged.

## Delivered invariant

The repository now contains one unregistered provider-local
`CodexRuntimeAdapter` for exact stable app-server 0.144.1 mapping. Construction
requires explicit discovery and runtime peers plus an independent fake-only
conformance authority. There is no default native peer, production authority,
composition import, registry entry, selector, dispatch path, or generic native
request escape.

Discovery is account-scoped and defensively captured. Create and exact resume
revalidate immutable selection, policy, posture, and native identity before
constructing a session. Resume never falls back to create. Complete persisted
history validates every generated stable `ThreadItem` shape but retains only
turn/item identities, which fence replay into later live turns.

One session admits one turn at a time. Exact thread/turn/item correlation,
ordered public agent text, terminal snapshots, interruption, cancellation,
iterator abandonment, cleanup, reuse, and disposal are fail-closed. Provider
prose, private reasoning, native identities, commands, paths, arguments,
results, diffs, and arbitrary metadata never cross the adapter. Context and
subscription quota remain typed unavailable.

Static dependency guards prove that production source cannot reach the adapter
surface and that the surface cannot reach Codex process owners, including
through transitive local helpers. Transitive child-process or alternate-
transport imports are also refused with an exact dependency path.

## Verification evidence

- Server typecheck plus focused Codex mapping/adapter, Claude shared-
  conformance, and static ownership suite: 99/99 passed.
- All 18 generated stable `ThreadItem` discriminants pass through complete
  persisted-history validation while only ordered identities survive capture.
- Final feature-tree `pnpm ci:check`: all workspace typechecks/tests, 660/660
  server tests, and dead-import guard passed.
- Production `pnpm --filter @pc-sdk/web build`: passed.
- Exact nine-path implementation stage, `git diff --check`, allowed-scope,
  excluded-path, native-vocabulary, and transitive dependency audits passed.
- Three independent hostile-review lanes report no remaining P0/P1/P2 finding.

One earlier full-suite attempt after the last P2 fixes reported 658/660 but its
truncated output did not retain the failing names. No source changed: an exact
server-suite diagnostic immediately passed 660/660, and the subsequent exact
full `pnpm ci:check` passed 660/660 plus dead-import analysis. The failed attempt
is retained here as inconclusive evidence and is not used as the sealing gate.

No Codex process, thread, turn, login, tool, MCP server, approval, context or
quota request, inference, credential-home access, PM/MCP network call,
stable-app data access, original-PC-SDK mutation, or external-repository
mutation was performed.

## Hostile review

Protocol/admission, lifecycle/race, and conformance/static reviewers drove
regressions for terminal and interrupt fencing; generator finalization; hung
peer waits; reentrant disposal; exact persisted-history shapes and data-only
capture; historical/live identity freshness; terminal snapshots; authority
independence; and both directions of the static dependency boundary.

The final quality review found three P2 closeout gaps: transitive process/
transport imports could evade the static guard, historical validators were not
compile-time exhaustive against the generated union, and draft evidence text
was premature. All three were repaired and the targeted re-review is clean.

## Deliberate boundary

CX-002 is provider-free conformance evidence, not native execution evidence.
Its fake-only continuation capability and policy receipts establish nothing
about a real Codex process. Credential freshness, entitlement, subscription
usability, billing route, model usability, native create/resume, effective
tool/MCP/approval enforcement, escaped-descendant containment, context or quota
acquisition, production dispatch, selectors, handoff, and the N5 real-fix gate
remain open. Experimental protocol and API-billed fallback remain unauthorized.

## Landing and cleanup receipt

Implementation `bf1f3a5ec8a12c17defa954c1bd5ccf3c59f4e87` and feature
record `98ce745db5e440a2f5c45fe2e620c00fda427dbf` guarded-landed as
merge `611c304eaa8932900b4f9d339edbb2058d99fa0f`. Its exact parents are
the unchanged base and feature record; base, contract, implementation, and
feature ancestry are positive. Feature and merge resolve to exact tree
`ce1c31e8ba38095a6e7571f2fe2705939436e645`.

Post-merge `pnpm ci:check` passed all workspace typechecks/tests, 660/660
server tests, and the dead-import guard. The production web build passed. The
merge was pushed and re-fetched exactly as `origin/main` before feature
teardown.

The clean feature worktree was deregistered only after exact tip, landing,
tree, remote, and zero-process proof. Git left a partially removed directory;
its exact parent/name, deregistration, absent `.git`, preserved branch tip,
landing ancestry, exact remote, and zero-process state were re-proved before
recursive removal. The feature branch remains preserved at the feature record.

Documentation closeout commit
`30fe8bb5604d68eb924f73bf2b40d0f4d4a71e3c` guarded-landed as merge
`a48db361026fc999bd4226797f329a85c0e795d7`. Commit and merge resolve
to exact tree `2bb0d86efee8316711985edb88a1022b6ad52cad`; exact parents,
closeout ancestry, and the six-path documentation-only scope are positive. The
merge was pushed and re-fetched exactly before teardown. The clean closeout
worktree was removed only after exact path, clean-tip, branch-tip, landing,
remote, and zero-process proof. Its branch remains preserved.

CX-002 is complete. Only the clean main checkout remained registered before
this final receipt update; the transient final-receipt worktree must itself be
guarded-landed, pushed, and removed without changing the completed product
evidence above.
