# CX-004 T3 typed native-resource ownership receipt

Date: 2026-07-19

Status: **passed — fake lab qualification only; production unavailable**

## Authority and scope

- Starting base: `06c578a7f8cb010adcdbf9875c4a70080fe19aa3`.
- Feature branch: `codex/cx-004-resource-ownership` (preserved).
- Sealed feature commit:
  `4f38e17667dc5399e9f8499a18b7115cbabd7aca`.
- Guarded landing:
  `bcbd89b1083b7e852f71284a2581e334173a054a`.
- Ordered landing parents:
  `[06c578a7f8cb010adcdbf9875c4a70080fe19aa3,
  4f38e17667dc5399e9f8499a18b7115cbabd7aca]`.
- Exact feature/landing tree:
  `8015a48db4b6d9532d1c14cf84f8fc22227b24b1`.
- The implementation commit changed exactly the 21 paths authorized by the amended
  T3 closure: `.gitattributes`, the native-source seal, the governing slice, and 18
  implementation/qualification sources. The package manifests, lockfile, workspace
  configuration, TypeScript configuration, and
  `packages/windows-containment/toolchain/**` remained byte-identical.

T3 implements only the typed native-resource ownership foundation. It does not add a
production native artifact, production composition, a provider process, a provider
credential/login-home access, an external-project mutation, an external PM write, or a
stable-repository mutation. No requirement is promoted by this receipt.

## Delivered behavior

- `native/resource-sites.json` is the sole declarative ownership registry. Its
  generator produces the tracked header and artifact-selected canonical manifest
  frame; the source guard rejects unregistered acquisition/release sites and raw API
  aliases outside the closed policy.
- Owned, borrowed, pseudo/no-release, and opaque resources use typed move-only
  wrappers and the exact lifecycle states `known-live`, `positively-released`, and
  `release-outcome-quarantined`.
- Borrow use and owner retirement share one atomic liveness word. Release cannot race
  an active borrow into reuse; moved-from state is irreversibly quarantined.
- Negative, before-call, and succeeded-but-uncertain release observations remain
  distinct. Uncertain release quarantines the value and carries the required
  process-creation poison/nonrestart-shutdown signal; no numeric value is reclosed.
- Recyclable-site ABA proof is bounded to 65,536 successful same-domain acquisitions
  or five monotonic seconds, whichever comes first, inside the 60-second
  single-process matrix. Exact reuse outside the bound is inconclusive, never pass.

## Seals and generated evidence

- Native-source manifest:
  `docs/execution/manifests/CX-004-native-source.json`.
- Source manifest SHA-256:
  `217e45f88d6acd5106142e49e978b2033e75dfbe6437dca30a43b90f1c27de8e`.
- Source-set SHA-256:
  `69093f4b01713e433accc32895f58e28ad4381d4ee9be36ebb9fcbf0c75b652a`.
- Closed source set: 18 files and 383,573 bytes. Every row length/hash matched on a
  clean post-landing checkout; all 18 files and the manifest have exact
  repository-owned `text eol=lf` attributes and zero carriage-return bytes.
- Generated ownership header: 50,240 bytes, SHA-256
  `3d3ad48db80c355d704b296187d3619f2a782d6ce60e49880a65b07562ac992e`.
- Source guard: 10 files, 11 typed markers, five raw calls, and four registered sites.
  Clean regeneration was byte-identical.

## Native qualification evidence

The official provider-free Windows T3 matrix ran with the pinned T1 toolchain after
removing every `NODE_*` environment variable and proving empty Node `execArgv`.

- Qualification PE: 223,744 bytes, SHA-256
  `10331155d7affbee478f985ae34ed52e58d67fdde0434996bf8251dc5f9c290a`.
- Embedded ownership frame: exactly one read-only PE frame, 5,682 bytes, SHA-256
  `bbb76322ef505b13730b70404e4e04d5c72a444d8d76cb0f0462a4da000921c2`.
- Canonical payload: 5,602 bytes, SHA-256
  `7148848a2f38eefe207a227fc1a4125eb7135cc42d37788a913b5beb16c78e8c`.
  Independent PE extraction byte-equaled the generated artifact-selected frame.
- Two isolated executions each passed 7,000,231 assertions. Their deterministic
  306-byte stdout receipts were byte-identical with SHA-256
  `0fcba9b19815d10c8a1972fde005093afab8aef1420c434933b5189ad9d38c2f`.
- The ABA canary positively observed registered site
  `qualification_owned_event` on attempt 1, then positively released it and rechecked
  the deadline.
- Unknown mode exited exactly 2 with empty stdout and stderr.
- Injected release-fault, borrow/release contention, move-state, manifest/parser,
  source-guard, raw-alias, profile-isolation, size/bound, and deterministic-repeat
  cases passed. The pinned native toolchain suite passed 97/97 and the focused
  TypeScript hostile suite passed 8/8.
- The full bootstrap, addon, and core release recipes returned typed
  `unavailable` with `productionAdmission:false` because later T4-T10 translation
  units are intentionally absent. No placeholder future translation unit was added.

The T3 fuzzer translation units compile the manifest and resource-state entry points.
Coverage-guided full embedded-frame fuzzing remains deliberately deferred to T11's
complete artifact matrix; this receipt does not claim that later gate.

## Repository verification and review

- Pre-landing and post-landing pinned-toolchain `pnpm ci:check` passed all 11
  workspace typechecks, every package test, 660/660 server tests, the dead-import
  check, and the process-boundary check.
- Post-landing `source-seal.mjs --check`, `resource-manifest.mjs --check`,
  `build.mjs --preverify`, and `build.mjs --release-availability` passed. T1 input root
  SHA-256 remained
  `8526c8b2955b6220a7955060aa8cebdddc7063840ce1e7c7ae2265e242df94f1`.
- `git diff --check`, exact path/secret review, native hostile review, scope review,
  and independent landing audit found no remaining P0/P1/P2 issue.
- The landing positively proved ordered parents, feature ancestry, and exact
  feature/merge tree equality. Local `main`, `origin/main`, and live
  `refs/heads/main` were re-fetched equal to
  `bcbd89b1083b7e852f71284a2581e334173a054a` after push.
- The landed feature worktree was clean and process-free. Git deregistered it but
  retained ignored dependency residue; the exact path, zero live owners, and all
  1,325 reparse targets remaining inside that worktree were proved before recursive
  removal. The feature branch remains preserved.
- Temporary T3 diagnostic scripts and native build roots were removed. No native
  qualification process or T3 worktree residue remains.

## Conclusion

`fake lab qualification only; production unavailable`

T3 is complete. The next safe action is T4: the serialized process-creation
coordinator and same-token, no-fallback `CreateProcessW` full-spawn path, using only
the separately recorded exact T4 closure in the governing slice. Restricted-token
parity remains T5; bounded full-duplex I/O remains T6; final fresh-Sandbox hostile and
coverage-guided qualification remains T11.
