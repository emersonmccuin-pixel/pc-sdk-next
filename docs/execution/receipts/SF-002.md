# SF-002 verification receipt

Date: 2026-07-12
Status: verified and ready to seal/land; seal, guarded landing, push, and
teardown pending

## Identity

- Base: `94dee1a7ec56ca3e2470769c9d136ed11754e6e6`
- Branch: `codex/sf-002-repository-lease`
- Worktree: `E:\Claude Code Projects\Personal\PC-SDK-Next-sf-002`
- Contract commit: `0be8912`
- Requirements: a bounded cooperative portion of `OPS-005` and `WT-004`

The sealed implementation SHA/tree and landing receipt do not exist yet. They
must be added only after the actual seal and positive Git landing proofs.

## Delivered invariant

For every configured Git-backed runtime cwd, PC-SDK Next now resolves the
native real path of Git's absolute common directory and acquires one
engine-lifetime, re-entrant repository lease before write-capable activity.
The lease combines:

1. a protocol-stable, SHA-256-addressed Windows named pipe or Linux abstract
   socket; and
2. a zero-wait SQLite `BEGIN IMMEDIATE` held in the Git common directory.

Main checkouts, subdirectories, filesystem aliases, and linked worktrees of one
repository converge on one identity. Distinct local repositories remain
independent. Same-engine builders may continue in separate worktrees, while
the existing in-process FIFO still serializes landing and stale-base
revalidation.

Project creation now carries the selected mode into the authority door and
revalidates the current filesystem state there. `init-empty` accepts only an
empty non-Git directory, `init-in-place` accepts only a nonempty non-Git
directory, and neither mode silently attaches a repository that appeared after
the UI probe. `attach-to-git` accepts an existing canonical worktree root and
refuses a selected repository subdirectory rather than rewriting the project
path to the discovered root. Mode/state drift and root mismatch return HTTP
409 before a project row or Git-side effect is created.

Initialization claims the anticipated `<project>/.git` identity before Git
starts, writes a crash-visible bootstrap-pending marker, and commits a clean
`Initial scaffold` for an empty directory or `Initial import` for adopted
files. The marker is removed only after Git resolves the anticipated common
directory and the initialized repository is clean; an incomplete marker is
typed unavailable and is never inferred safe from age or process absence.

The authority guard covers fresh provision, setup/readiness, continuation and
automatic continuation, runtime create/resume, deliverable sealing,
verification, review checkout/runtime/reclaim, accept-time revalidation,
landing, teardown, branch deletion, orphan pruning, and recovery. Because the
pinned native permission mode is not a read-only sandbox, admission also covers
orchestrator, payload, and review runtimes whose cwd is Git-backed.

Every fresh repository run durably stamps an exact `git-common-dir-v1`
receipt. SQLite guards require a complete shape, freeze it after insertion, and
require continuations to inherit the exact parent receipt. Retained legacy
receipts remain readable evidence but cannot be promoted into mutation
authority. Project-path or worktree-path retargeting is rejected before a
later door can read or persist foreign Git evidence.

Project rows are born with or atomically bind that same durable identity, and
worktree receipts carry it through the run. Boot recovery and landing pass the
stored receipt as an expected identity during acquisition, then revalidate it
under the live guard before classifying, merging, pruning, or cleaning up.

Ambient `GIT_DIR`, `GIT_WORK_TREE`, and `GIT_COMMON_DIR` are removed at all
three app-owned child seams: direct Git commands, setup/readiness/verification
shell commands, and provider runtime environments. Repository selection is
therefore cwd-derived at the same path whose authority was proved.

Migrated projects that have a nonempty folder but no repository identity fail
native continuation honestly. Historical availability/resume and active
restart remint return typed `repository-identity-unavailable` before runtime
preflight, durable continuation-state changes, or active-session replacement.

The app-owned sibling worktree root must be a real directory. A symlink or
Windows junction cannot redirect provision, review checkout, teardown, or
recursive orphan cleanup outside that root.

## Focused evidence

- Repository lease suite: 19/19 passed. It covers canonical aliases and linked
  worktrees, same-engine single-flight/re-entry, awaited/idempotent release,
  mode-aware initialization and attach, initial scaffold/import, incomplete
  bootstrap refusal, expected-identity drift, corruption/permission/alias
  state, real child-process contention, distinct repositories, graceful
  handoff, hard kill and simultaneous reclaim, losing-side non-mutation,
  occupied continuation/landing/teardown, and per-repository boot recovery.
- Runtime session selection: 17/17 passed, including historical availability/
  resume and active remint refusal when project identity is unavailable.
- HTTP contract: 12/12 passed, including authoritative mode/state drift,
  existing-repository refusal for both init modes, clean `Initial import`,
  durable project identity, canonical-root attach, and subdirectory refusal.
- Landing + independent review + kill recovery matrix: 47/47 passed
  (27 landing guards, 12 full-review cases, and 8 kill-recovery cases),
  including expected-identity acquisition at boot and landing.
- Dispatch guards 25/25, dispatch lifecycle 4/4, worktree profiles 5/5, and
  durable specialist identity-stamp guards 3/3 passed.
- Full server regression suite: 370/370 passed.
- Full feature-tree `pnpm ci:check` passed, including workspace typechecks,
  all package tests, and the dead-import guard. `git diff --check` also passed.

The Windows host could not create a file symlink without elevated privilege
(`EPERM`), so the lease-file symlink state also has a deterministic injected
guard. A real Windows junction was created and rejected for the recursive
worktree-root boundary. Fixture cleanup now awaits process-wide lease release
before removing repositories and sibling worktree roots. Exact emitted paths
were absent after the focused runs; no repository-lease fixture process or
`pc-sdk-repository-lease-*` root remained.

A direct migration diagnostic applied `0015_repository_identity.sql` over a
pre-existing stamped legacy receipt. The row survived unchanged; promotion was
rejected; new malformed stamped receipts were rejected; and retained legacy
rows remained readable. The migrated legacy boot-quarantine guard also passed.

No live provider turn, quota acquisition, PM/MCP call, external repository
mutation, stable-app data mutation, or original-PC-SDK source mutation was
required.

## Hostile review

Two independent hostile review lanes drove fixes for:

- unleased orchestrator, payload, and independent-review runtime doors;
- paused-review revival before repository admission;
- sealing one path after validating another;
- accept-time reads from an unvalidated mutable project path;
- mutable durable repository receipts and forged continuation inheritance;
- legacy review-checkout reclaim without an immutable receipt;
- symlink/junction redirection of recursive worktree cleanup;
- same-manager admission and release/validation races;
- project creation mode drift, silent attach, and subdirectory root mismatch;
- bootstrap failure/release poisoning and incomplete-marker handling;
- ambient Git selector redirection of Git, shell, and provider children;
- missing expected-identity checks at boot recovery and landing;
- historical and active-remint continuation without durable project identity;
- fire-and-forget repository fixture cleanup;
- false continuation completion from an inherited parent deliverable; and
- lifecycle `completed` preceding branch-delete/orphan-sweep settlement.

Both independent hostile re-reviews are clean after those fixes and report no
remaining P0/P1/P2 blocker. Together with the green full feature-tree gate,
SF-002 is verified and ready to seal/land. This receipt still does not claim a
sealed tree, merge, push, or teardown before those actions occur.

## Deliberate boundary

SF-002 is a cooperative same-host protocol. The preserved working PC-SDK does
not implement it, nor do manual Git/IDE processes or unrelated tools. The
manual prohibition on configuring the working app and PC-SDK Next for
write-capable work against the same external repository at the same time
remains mandatory.

The lease belongs to the server process. A provider, setup, verification, or
Git child that survives a hard-killed server is not contained by this receipt.
No supervisor/helper process was introduced. SF-002 removes only the three
ambient Git repository selectors at Git, shell, and provider seams; `SEC-003`
still owns the general child-environment allowlist/secret scrub, and later
process hardening owns escaped-child containment. Remote-host and network-
filesystem exclusion also remain outside this slice.

Consequently `OPS-005` remains `accepted`, not globally `verified`, and SF-002
does not claim the full N4 process-failure gate. `OPS-006` loopback binding and
N7 daily-driver work remain separate.

## Closing work

Before SF-002 can be marked complete:

1. the complete diff and actual changed paths must receive the final seal
   audit;
2. a sealed feature commit/tree must be recorded;
3. guarded merge, ancestry and exact tree proof, post-merge `pnpm ci:check`,
   push to exact `origin/main`, and feature-worktree teardown must pass; and
4. this receipt must be updated with those immutable Git and gate receipts.

After that landing, the next safe N4 action is a separately contracted
`SEC-003` child-environment allowlist/scrub slice. `OPS-006` stays ordered in
N7 and must not be bundled into that work.
