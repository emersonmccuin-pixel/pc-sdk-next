# CX-004 Windows Sandbox runner-readiness receipt

Date: 2026-07-14
Status: Q0S passed; run-evidence closeout `R0` is prepared for guarded landing and
the separate post-landing documentation closeout `R1` remains required before
native/TypeScript product implementation.

## Scope and identity

- Scope: `runner-readiness-only`; this is not the final CX-004 fake-product matrix and
  is never production admission.
- Requirements: none promoted by Q0S.
- S0 feature commit:
  `de5d7ee95b0eea2f5f0f49c24cc699f850896ffc`.
- S0 guarded landing:
  `4fd90a4c9647af000d051beb59c3013222d22461`.
- S0 ordered parents:
  `[a77268b9ffc47f8235589666a6a462ddabc2fd6d,
  de5d7ee95b0eea2f5f0f49c24cc699f850896ffc]`.
- Exact feature/merge tree:
  `91281255c85ce84ae44046185b548a5183cb0c74`.
- Tracked source-seal manifest SHA-256:
  `af6835a45b365cb74ced0fe5b380512a19f58092e92d70b431cc7c7077888bff`.
- Closed local-evidence bundle id: `dd17bb6a67e44d2b8fef6a7f6dab1a63`.
  Its canonical local root remains untracked.
- Closed bundle-manifest SHA-256:
  `e7aa3420516490e154c649313c562e399b5c6bbc658440fd0bc5729d65345025`.
- Local tracked-receipt JSON SHA-256:
  `e2bb20b78e6a1efad009c1a67932058100aa392dd4de4a1d15d63f00e682cbfc`.
- Receipt schema: `cx004-q0s-v1`; outcome: `passed`; teardown level:
  `sandbox-session-stopped`; host-smoke scope: `host-smoke-only`.

The S0 merge passed the focused 981-assertion Sandbox contract, bounded host smoke,
`git diff --check`, independent hostile review, and full pre/post-merge
`pnpm ci:check` with 660/660 server tests plus the dead-import guard. Local `main`,
`origin/main`, and the live remote were exactly equal to S0 before Q0S started.

## Tracked semantic receipt

| Fact | Accepted value |
| --- | --- |
| Host | Windows 11 Pro/Professional Client AMD64 `26200.8655` |
| Sandbox package | `MicrosoftWindows.WindowsSandbox_0.5.3.0_x64__cw5n1h2txyewy` |
| Sandbox/CLI version | `0.5.3.0` / `0.5.3.0` |
| Guest | Windows 11 24H2 Enterprise Client AMD64 `26100.8655` |
| Guest process architecture | `AMD64` |
| Guest product type | `1` |
| Guest integrity/groups/privileges | `high` / `13` / `24` |
| Template SHA-256 | `19d42900ff88259254f297c16076a58e7b9c8b807ad504d6afdf9801d84b5115` |
| Stable manifest SHA-256 | `87f880113529e11ec58058e6ed59c07f726002f92c9c0c45a569a6d2ff962625` |
| Fresh session runs | `2` |
| Network isolation | `true` |
| Input mapping read-only | `true` |
| Persistence reset | `true` |
| Bounded host smoke | `passed` |
| Requested networking/vGPU/clipboard | `disabled` / `disabled` / `disabled` |
| Requested audio input/video input/printer | `disabled` / `disabled` / `disabled` |
| Requested protected client/memory | `enabled` / `4096 MiB` |

The raw compatibility registry label `ProductName="Windows 10 Enterprise"` is retained
inside local evidence and interpreted only as the exact Windows 11 24H2 guest tuple
above. It is not a Windows 10 marketing-identity claim.

## Lifecycle and boundary evidence

- Two fresh sessions returned distinct canonical ids without tracking either id.
- Each session produced challenge-bound terminal evidence, reproduced the stable input
  facts, positively denied both new-file creation and write-open of an existing mapped
  input through the exact admitted PowerShell exception chain, and left the input bytes
  unchanged.
- Canonical rendered XML retained every requested network/device restriction; guest
  negative probes and both dynamic host canaries are the observable evidence and
  observed no prohibited connection.
- Each exact session id was positively stopped. Mapping release was proved, the final
  modern-CLI session list was empty, and clean relaunch proved the first session's
  persistence canary absent before creating the second challenge-bound canary.
- Source seals before/after relevant execution points retained bounded regular bytes,
  rejected nonordinary index flags, and matched computed Git blob ids to caller-pinned
  S0 `HEAD`.
- The bounded cooperative host Job Object smoke passed without becoming production or
  physical-power-loss evidence.

Raw machine/user names, SIDs, session ids, challenges, absolute profile paths, boot
timestamps, and native command output remain only in the closed local bundle. This
tracked receipt contains only its aggregate binding and nonpersonal semantic facts.

## Limitations and next gate

Q0S qualifies only the provider-free Windows Sandbox runner. It does not qualify an
immutable custom image, protected production install, physical power loss, a provider
process, a real principal, a real project, or production native execution. The prior
development bundles remain immutable inconclusive evidence and are not reclassified.

Guarded-land and push/re-fetch this `R0`, then land the separate documentation-only
`R1` recording exact R0 commit/tree/parents/merge and remote equality. Product
implementation remains blocked until `R1` is exact.
