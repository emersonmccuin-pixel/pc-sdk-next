# CX-003 native-execution safety decision receipt

Date: 2026-07-13
Status: accepted — the product owner explicitly approved ADR-0002 as proposed after
the verified proposal checkpoint. Acceptance is recorded; guarded landing, push,
re-fetch, feature teardown, and the separate final-receipt closeout remain pending

## Identity

- Base: `7259645dfaa9bb4c071843819119dda319d4cea8`
- Branch: `codex/cx-003-native-execution-decision`
- Worktree: `E:\Claude Code Projects\Personal\PC-SDK-Next-cx-003`
- Proposal checkpoint: `4fbbdf0f77b447e78f4218816e90d553ed93145a`
- Proposal tree: `781cd1ff8beae1ef046687661d336049496b0acc`
- Contract: `docs/execution/slices/CX-003.md`
- Evidence: `docs/research/codex-native-execution-safety.md`
- Accepted decision: `docs/decisions/0002-codex-native-execution-safety.md`
- Requirements: none promoted

## Proposal evidence

Stable Codex app-server 0.144.1 cannot supply the closed-world, independently
verified effective-policy receipt required for native execution. A qualifying
future stable version must provide a quiescent two-step admission barrier and
an immutable policy epoch bound to the exact selection, continuation attempt,
thread, effective provider auth identity, native account/workspace, auth mode,
plan, service tier, subscription-versus-API billing route, external-action
inventory, sandbox/network/filesystem policy, approval behavior, and
configuration provenance. Any bound provider identity, account, workspace, auth
mode, plan, service tier, billing route, runtime, model, or effort drift requires
a new app session; unknown actions always deny.

Windows containment is independently feasible through two repository-owned native
owner-TCB artifacts—an exact transient cold-start/controlled-restart bootstrap and C++
Node-API full-spawn addon—plus the separately admitted pinned `better_sqlite3.node`
privileged PE. A live future N7 launcher must retain non-authoritative same-PE `entry-
scrub` through exact success or terminate/wait, but its process handle does not cover N7
death/hard kill before scrub's watcher arms. That create→arm race remains a separate
production blocker requiring a crash-safe kernel anchor and topology receipt; CX-004
neither qualifies nor freezes it. On a provably unjobbed internal fixture, scrub arms
its independent watcher, then main creates/configures/queries/publishes the process-
sole permanent outer job with watcher ack before atomic hard-gated `cold-bootstrap`
create. A causal two-phase event orders ready, least job-target return, cold protect/
query/publication, and ack. Every watcher-visible handle retires/quiesces; protected
transient sources are cleared and queried clear before source close and the one scrub-
success exit. From positive watcher arm, hard deadlines
terminate the exact transient/job. Cold requires that success and exact sole outer-job/
unjobbed-parent proof before SF-001. Direct/replay/wrong-job/
inherited-handle cases fail; pre-existing same-user handle racing remains outside the
descendant-origin threat.
Cold-bootstrap immediately protects/queries/publishes its SF-001 source witness, then
launches exact sealed `node.exe` against a deterministic protected
precompiled JavaScript boot entry by implicit inheritance of that permanent outer job,
then gaplessly hands off witness/job ownership. The current `cmd`/`pnpm`/`tsx`/esbuild/source-loader path is
development-only and non-admitted. Cold start opens no SQLite/product state before
bootstrap exit; in controlled restart only new owner may acquire the dedicated
admission-SQLite lease while bootstrap lives, acknowledge, then let bootstrap exit
before product DB open and exact restart CAS.
Restart transfers the exact committed nonce/generation digest into new owner, binds it
to successor admission, and never selects a generic/latest row. The owner-lifecycle
component atomically commits each restart row plus canonical outbox/control event under
sealed SQLite crash/power-loss semantics; a post-bootstrap live hang keeps the witness
occupied and makes contenders refuse, while crash/death permits a successor.

The owner addon is first optional non-host/non-system native code. A full-lifetime
native-load gate admits the exact sealed Node host/system/KnownDLL import/delay/dynamic-
load closure and, among optional application-native mappings, only that addon then the
exact SQLite binding; one owner SQLite factory injects the
same verified addon into every DB constructor, bans extension loading, and excludes
optional `ws` native accelerators. Exact package/lock/store/integrity pins, native-
dependency/final-PE/provenance, hardening/static-analysis/sanitizer/fuzz, and checked
input/I/O/IPC bounds are qualification facts. A closed process-spawn inventory from
the admitted bootstrap through the Node owner eliminates or full-spawn-replaces the
current restart, Git/action, Claude, live stdio-MCP, and source/dev-only Codex edges; a
future production Codex binary is separately supplied/qualified, and dormant/install-
only edges are excluded. The external one-click UI launcher/browser remains N7:
routine entry-scrub invocation/lifecycle wait/terminate/close is constrained, but its
process handle is not the required crash-safe anchor, and the browser is never owner-
jobbed.

The no-fallback full-spawn family uses same-token `CreateProcessW` or restricted-token
`CreateProcessAsUserW` with suspended creation, sealed ordered job vectors, exact
handle lists, restrictive process/thread descriptors, mitigation/child/security-
capabilities attributes, and protected noninteractive station/desktop policy. It
serializes process creation, requires positive pre-resume queries and
ordered broad job/`PROCESS_INFORMATION` source retirement,
`ResumeThread(rootResumeThread) == 1`, and positive derived-resume-handle close. It uses
a generated typed-wrapper/static-guard/PE-digest manifest for every owner, borrower,
and no-release/pseudo native-resource site and tracks owned resources as
live/released/quarantined, never reuses an uncertain-close numeric handle, poisons on temporary-handle uncertainty,
and produces
normal/external-hard-kill plus bounded full-duplex/teardown-I/O receipts. Ordinary addon
launches own one new leaf, borrow rather than close session/outer parents, prove every
retained-job baseline, and set zero nested-job UI restrictions. The provider-session
leaf alone retains an exact lifecycle-plus-assign-only handle pair held solely by the
PC-SDK owner; teardown quiesces action-handle use and closes assign first, lifecycle
last. Action/transition/Git leaves retain lifecycle only. A hostile resumed root
may self-create an unnamed inner job; that unobservable total-chain change is an
accepted contained denial-of-service residual, not an escape or exact topology claim.
The outer job nests above provider session, action, transition, and Git jobs. CX-004 proves fake-
principal pipe/job/load/restart canaries only; the complete future provider/lower OS
boundary and hostile owner/job-handle cross-process acquisition remain later receipts.

The accepted initial platform is one exact qualified Windows 11 25H2 x64 client
full-revision/UBR/identity tuple within base build `10.0.26200`. The complete
admitted-path/hard-kill matrix requires a pinned disposable runner
whose native product/build/architecture probe and immutable image provenance/
digest, including exact full revision/UBR, are sealed in CX-004's contract before
implementation. Production runtime must equal every admission-checkable native
OS/security-component identity/provenance field; any delta is typed unsupported
until full CX-004 requalification. `windows-latest` independently records
observed identity as an always non-admitted lane and proves only source-build
plus the typed-unsupported path; an identity match fails for explicit review.
Every other host is unsupported. CX-004 may fake-provision the protected OS/image/
owner-nonwritable install/postmortem baseline only in its pinned disposable runner.
Production native admission remains unavailable pending a separate provisioning/
packaging/N7-launcher-lifecycle decision; no installer, elevation, WDAC/AppLocker mutation, or runtime
self-ACL seal is authorized. Future distribution requires the sealed precompiled boot,
verified prebuilds, both repository-owned PE manifests, the pinned SQLite PE manifest,
and complete native/process-load closure.

## Verification evidence

- Exact changed-path audit: six allowed documentation paths and no source,
  package, lock, workflow, launcher, database, runtime, UI, or composition path.
- Stable schema audit: all 43 exact generated anchors named by the evidence
  document exist under the checked-in binary-matched schema root.
- Governing authority audit: all ten named architecture, requirements,
  execution, state, and protocol documents resolve.
- Reproducible server production-closure audit: recursive canonical-path parsing of
  `pnpm --filter @pc-sdk/server list --prod --depth Infinity --json` found 160 unique
  roots, 153 present, and seven absent non-host Claude optionals. Present production
  roots contain exactly `claude.exe` (248,682,144 bytes, SHA-256
  `d5072b25b9a20bffb24625d36129a05ed2be4d2eb7e35625aad6aa35596892c2`) and
  `better_sqlite3.node` (1,721,344 bytes, SHA-256
  `c6770a96c516d2b3e78308ecfcc146c38c0eb0875e180c8028d77f1e93914c6d`). The
  16-file installed-native scan classifies the other 14 as dev/build artifacts; Codex
  0.144.1 has zero production entries and its future production binary requalifies.
  The install-only chain is `better-sqlite3@11.10.0` → `prebuild-install@7.1.3`,
  `detect-libc@2.1.2`, and `node-abi@3.94.0`; its ambient unversioned `node-gyp`
  fallback is absent locally and excluded from the admitted runtime.
- Explicit trailing-whitespace scan over all six proposal files: passed. Staged
  `git diff --cached --check` passed with exactly the six authorized cached paths—two
  modified and four added—so all new files are included.
- Full feature-tree `pnpm ci:check`: all workspace typechecks/tests, 660/660
  server tests, and dead-import guard passed.
- Three independent exact-hash hostile lanes—static/source and dependency proof,
  cross-document architecture/scope consistency, and containment/decision safety—
  recomputed the same six-file snapshot and each reported no P0/P1/P2 finding.

No Codex process, thread, turn, login, tool, MCP server, approval, inference,
credential-home access, provider call, raw API, stable-app data access,
original-PC-SDK mutation, external-repository mutation, or requirement
promotion occurred.

## Accepted decision

On 2026-07-13, after the exact proposal checkpoint and its three clean hostile
reviews, the product owner explicitly approved ADR-0002 as proposed and accepted all
three choices together:

1. two repository-owned native artifacts plus the separately admitted pinned SQLite
   PE; internal same-PE scrub→cold-bootstrap proof from positive scrub-watcher arm on a
   provably unjobbed fixture; main-path outer-job create/configure/query/publication with
   watcher ack; atomic cold assignment; causal two-phase ready/job-target/ack; all-
   watcher-handle retirement/quiescence; protected-source clear/query/close; exact
   parent-success exit; transient self-deadlines and phase-complete hang canaries; and
   exact sole-owner-job/unjobbed-parent proof. N7 create→scrub-arm crash/death/hard-kill
   and any changed topology remain a separate production blocker whose routine process
   handle is insufficient. Accept the explicit ambient-malware and hostile post-resume
   self-nesting-as-contained-denial-of-service residuals; exact protected plain-Node
   compiled boot; closed full-lifetime
   native and admitted bootstrap/owner-runtime transitive process-spawn inventories
   with external N7 UI launcher/browser separation; one SQLite binding factory with extension/
   optional-native denial; the no-fallback full-spawn family and its implicit/borrowed/
   new-leaf job ownership, provider-session-only exact lifecycle-plus-assign-only pair
   with assign-first/lifecycle-last cleanup, exact least-rights job/process/thread
   derivation before ordered broad job/`PROCESS_INFORMATION` source retirement and
   derived-resume-handle retirement before publication, the generated closed owner/
   borrower/no-release native-resource site manifest plus global release-state/
   quarantine and exhaustive release-fault/non-
   vacuous exact-value ABA matrix where numeric reuse applies, zero nested-job UI
   restrictions, parent-
   baseline cleanup,
   and atomic security/creation/UI attributes; protected SF-001 pipe/outer job;
   controlled restart with
   exact tuple, row/outbox, and power-loss semantics; hardening/bounds/Windows CI; fake-
   only CX-004; and production unavailable pending a separate protected-install/OS-
   policy packaging/N7-launcher-lifecycle decision;
2. one exact qualified Windows 11 25H2 x64 client full-revision/UBR/identity tuple
   within build 26200, a pinned native/image runner, runtime equality, independent
   non-admitted `windows-latest`, and unsupported behavior on every delta; and
3. stable-upgrade/wait with full requalification, a quiescent two-step admission
   barrier, immutable policy epoch, and all-origin empty external-action first gate,
   with no fork/experimental/wire/raw-API/detect-and-abort bypass.

This acceptance authorizes only fake-process/fake-principal CX-004 after the feature and
separate final-receipt closeouts both prove guarded landing, push, re-fetch, and
teardown. It authorizes no production native admission and no Codex process, login-
home access, or invocation.

Before any CX-005 provider process, login-home access, or invocation, a separately
approved fresh production receipt must bind the exact host/build, protected install/
code-integrity/postmortem baseline, plain-Node compiled boot and pre-addon zero for
unlisted spawned processes or optional non-host/non-system native mappings,
owner-addon/SQLite/load/factory/native/process closure, SF-001 protected pipe, full-
spawn family, and cold/restart outer-job handoff/state/recovery. No fake lab receipt
substitutes. Credential-free evidence must then select the provider-root TCB versus
opaque-broker boundary and positively qualify every root-applicable attester, token,
process/thread/object/UI, filesystem/created-object, diagnostic/WER/audit/content-
inspection, shared-home/auth/network/export, IPC/handle, topology/generation/recovery,
and preventive-resource invariant before credentials. An unproved residual remains
unavailable or needs a separate explicit product/security decision.

CX-007 remains provider-free non-process compilation. CX-008 revalidates the entire
root receipt and adds lower/sibling/standalone credential-free transition/restricted-
Git composition: exact principal tokens and atomic creation attributes; executable/
config/parser/native closure; diagnostics/content inspection; private volume/path/
stream/alias/object semantics; capability export; per-target recovery; owner/session/
action/transition/Git hierarchy; provider generation/prior-zero; and preventive lane/
global budgets. The initial lower template is leaf-only/network-empty; process spawning
needs a separately accepted mediated-child or stronger-isolation topology and each
process remains independently receipted. The full composed CX-004 matrix, cleanup,
recovery, power-loss, and VM-teardown proof must pass with no real credential/data/
project.

Git delivery is exactly restricted Git candidate creation, then a memory-safe owner
verifier, then owner-only verified-byte promotion and files-backend expected-old/new
ref CAS. Admission pins exact Git version/format/object algorithm/files backend and
excludes hostile config/alternate/hook/filter/helper/worktree/alias states. Disposable
abrupt-reset/power-loss proof covers object/ref/reflog/rename/flush edges. Lower
principals never receive common-object-parent create or target-ref rights, and no
owner-side general Git parser/broker is exposed.

CX-009 and CX-010 each require matching fresh provider and independently attested OS
receipts. The OS receipt exhaustively carries current production boot/addon/SQLite/
pipe/full-spawn/restart/hierarchy/generation; trust/broker/shared-home; every root/
lower/transition/Git token/executable/UI/diagnostic/inspection/filesystem/target/export/
resource/recovery/IPC/lifecycle fact; exact Git format/backend/parser/verifier/
promotion/CAS digests; attester; and qualification equality. Stable transition/Git
templates, implementations, recovery epochs, and positive no-active-instance state are
deployment facts; each actual generation/process/job is per-call. A challenge-bound
composite CAS verifies every field, zero unresolved leases, and no oversubscription
before action. CX-009 is disposable parity only. CX-010 repeats the complete fresh join
before real mutation, and delivery/landing requires the restricted-candidate/verifier/
promotion/ref-CAS snapshot/commit/tree/parent/scope/secret receipt bound to current
composite/action evidence. Lab/prior-attempt evidence never substitutes; drift fences
or quarantines.

The acceptance is recorded in this worktree and must now be sealed in a decision-record
commit. Then revalidate its clean tip, guarded-land/push it, prove post-merge and exact
remote state, and remove the feature worktree. A separate docs-
only final-receipt worktree then records exact proposal/decision identities, gates,
landing/push/re-fetch proof, and feature teardown; it must itself be guarded-landed,
pushed, re-fetched, and removed before any accepted CX-004 work begins.
