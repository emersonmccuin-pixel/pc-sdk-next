# PI-001 verification receipt

Date: 2026-08-16
Status: complete — implementation sealed and verified; landing is a local
fast-forward of this closeout tip onto `main`

## Identity

- Base: `48d2dcd685aa2cba1a5dcc56fb7ddeac6af2ac31`
- Branch: `codex/pi-001-project-instructions`
- Worktree: `E:\Claude Code Projects\Personal\PC-SDK-Next-project-instructions`
- Sealed implementation: `7cf8fe47023452a33cf982684660a7df3e36a9ca`
- Sealed implementation tree: `6c2f9d16b177ca56ef39c4129e9cb0a84566d1b7`

## Delivered invariant

PC-SDK owns one root `AGENTS.md` instruction source. Before a Claude or Codex
orchestrator/specialist native session is minted, the app reads a bounded,
canonical UTF-8 file, hashes it, binds the immutable snapshot to the app
session or agent run, and injects the composed text through the provider-neutral
runtime contract. Re-mints and native continuations retain their original
snapshot; newly created app sessions see later file revisions.

Codex receives `project_doc_max_bytes: 0`, so its native discovery cannot
duplicate or independently change the app-owned snapshot. Claude receives the
same compiled text through its system-prompt seam. Missing `AGENTS.md` is an
explicit empty snapshot; malformed, oversized, aliased, replaced, or invalid
UTF-8 sources fail closed. The tracked `CLAUDE.md` forwarding stub and the
Claude-specific stock-agent instruction were removed.

## Verification evidence

- `pnpm ci:check` passed: all workspace typechecks and tests, 825/825 server
  tests, dead-import validation, and process-boundary validation.
- Focused server project-instruction tests passed 4/4 after the final
  file-identity hardening; server typecheck passed.
- Focused Claude/Codex adapter suite passed 92/92 before the final loader-only
  hardening; the affected loader tests were rerun afterward.
- Focused DB project-instruction tests passed 2/2, including new-session
  revision adoption and exact parent-snapshot inheritance by continuation.
- A real pinned Codex app-server session minted and disposed successfully with
  model `gpt-5.6-sol` and no inference turn.
- `git diff --check` passed and only root `AGENTS.md` remained in the
  repository instruction-file inventory.

No provider inference turn, external PM/MCP write, stable-repository mutation,
or stable-app data access was performed.
