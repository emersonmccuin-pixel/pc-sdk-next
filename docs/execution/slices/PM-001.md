# PM-001: AInativePM ownership and integration discovery

Status: active — defined and prepared for read-only evidence gathering

## Objective

Complete the deferred N1 joint inspection of AInativePM's code, domain,
persistence, UI, REST, and MCP surfaces together with every existing PC-SDK
touchpoint. Produce an evidence-backed ownership seam, typed integration-port
proposal, failure/idempotency policy, and rejected-duplication list before either
application gains new PM synchronization behavior.

Discovery is read-only across both repositories and the live AInativePM
surfaces. It may end in a product-direction decision, but evidence gathering and
proposal synthesis do not require that decision up front.

## Requirements and boundaries

- Requirement IDs: `PM-001` through `PM-003`, the AInativePM standing case in
  `MCP-001` and `MCP-002`, and the external-side-effect boundary in `SEC-002`.
- Boundary documents: `docs/master-plan.md`,
  `docs/architecture/boundaries.md`, `docs/research/ainativepm-discovery.md`,
  `docs/pm-anchoring.md`, and `docs/worktree-lifecycle.md`.
- AInativePM owns its domain model and long-lived intent/management truth.
  PC-SDK owns technical execution, contracts, runtime evidence, verification,
  repository delivery, and their receipts. This slice determines the exact
  reference/command/event seam; it does not assume the provisional anchor design
  is correct.

## Dependencies

- PC-SDK Next base: clean pushed `main` at
  `a7c5423cb5a4aa8549521badcfdd320437a74def` after RS-004 closeout.
- Feature branch: `codex/pm-001-ainativepm-discovery`.
- Feature worktree:
  `E:\Claude Code Projects\Personal\PC-SDK-Next-pm-001`.
- AInativePM source: clean pushed `main` at
  `E:\Claude Code Projects\AInativePM`, commit
  `5033d5e` (`github.com/emersonmccuin-pixel/ai-native-pm`). It remains
  read-only; its trunk-based write rules are not exercised by this slice.
- AInativePM's current architecture is one generic item/type primitive with
  behavior-driven items, nested domains, shared core operations behind UI and
  MCP, and an admin-tool visibility gate. These are starting observations, not
  yet an approved PC-SDK integration design.
- Both PC-SDK instances share the same user-scope AInativePM remote and
  credentials. No automated PM write, folder registration, admin-tool toggle,
  token change, or test mutation is authorized.

## Scope

In scope:

- AInativePM domain and persistence map: generic items/types/behaviors, domain
  hierarchy, membership, assignment, stages/status, decisions, context,
  attachments, links, rules, views, templates, calendars, and durable events;
- UI/REST map of how a person creates, organizes, assigns, discusses, reviews,
  completes, archives, and revisits work across software and non-software
  domains;
- MCP map: exact commands/queries, shared-core parity, auth/account/project
  resolution, hidden admin surface, idempotency/retry behavior, health,
  unavailable/degraded states, and current consumers;
- PC-SDK map: `pmRef`, contracts, agent runs, pending asks, deliverables,
  verification/landing evidence, prompts, MCP registry/bridge, project context,
  and every provisional anchoring assumption;
- one ownership table stating which system owns status, assignment, decisions,
  progress, evidence, identity, deletion, and disagreement resolution;
- interaction sequences for reference/dispatch, progress/evidence, completion,
  retry/duplicate delivery, PM outage, deletion, and conflicting state;
- a provider-neutral typed PM port proposal with explicit query/command/event
  separation, idempotency keys, positive receipts, degradation, and approval
  boundaries;
- explicit classification of future updates as automatic, suggested, or
  user-approved, plus deep-link/reference direction without copied truth;
- revalidation and supersession of `docs/pm-anchoring.md`.

Out of scope:

- changes to AInativePM source, database, configuration, folders, tools,
  settings, auth, Railway deployment, or Git state;
- live or local create/update/archive/register/unregister/admin-tool operations;
- PC-SDK schema, runtime, MCP manager, prompt, UI, contract, or dispatch changes;
- implementing synchronization, anchoring, webhooks, polling, deep links, PM
  writes, or a general work-item system;
- reading secret values from `.env`, credential stores, or databases when code
  and schema evidence is sufficient;
- accepting an ownership/product proposal on the user's behalf.

Allowed tracked paths in PC-SDK Next:

- `docs/execution/slices/PM-001.md`
- `docs/execution/current.md`
- `docs/current-state.md`
- `docs/research/ainativepm-discovery.md`
- `docs/pm-anchoring.md`
- `docs/architecture/boundaries.md`
- `docs/requirements.md`
- `docs/master-plan.md`

No tracked or untracked AInativePM path may be changed. Any additional PC-SDK
path requires an explicit scope note here before editing.

## Acceptance criteria

1. The source receipts fix both repositories and show clean/pushed state before
   inspection; the final path audit proves AInativePM stayed unchanged.
2. The deliverable maps AInativePM's generic domain/persistence model and its
   real UI workflows without reducing the product to software work items.
3. REST and MCP are traced to shared core operations, including auth/scope,
   hidden admin tools, health, retries, idempotency, and typed failure behavior.
   Unknown or absent behavior is recorded as unknown, not inferred.
4. Every current PC-SDK PM seam is mapped from public contract through durable
   state, dispatch/prompt use, safe projection, and MCP composition. `pmRef`
   remains a reference, never silently promoted into copied PM truth.
5. One ownership table assigns long-lived intent, hierarchy, status, assignment,
   decisions, discussion/context, execution state, verification, Git delivery,
   artifacts/evidence, deletion, and audit provenance to exactly one authority
   or an explicit reference boundary.
6. Interaction sequences cover happy path, duplicate/retry, timeout, outage,
   stale/deleted references, conflicting state, cancellation, and recovery.
   Timeouts and missing evidence never imply PM success.
7. The typed port proposal separates queries from side-effecting commands,
   requires idempotency keys and positive receipts for every future write, and
   defines visible unavailable/degraded/inconclusive states.
8. Proposed automatic, suggested, and user-approved actions are explicit. No
   automated PM write is recommended without a proven ownership and replay rule.
9. The provisional folder-binding/token/explicit-project anchor assumptions are
   individually verified, rejected, or left unknown with evidence; the old
   implementation proposal is superseded rather than silently retained.
10. Independent domain, UI, MCP, and PC-SDK-touchpoint reviews converge into one
    proposal. Read-only browser/MCP checks expose no secrets in the receipt and
    leave the remote unchanged.

## Verification

- Startup: clean/pushed Git receipts for PC-SDK Next and AInativePM; exact
  branch/worktree identity; no dependency installation required for this
  documentation-only slice.
- Focused: source references for domain/schema/core operations, REST/UI, MCP
  handlers/server instructions/auth, and PC-SDK `pmRef`/MCP seams; bounded
  read-only UI/MCP observations where code alone cannot establish behavior.
- Closing: cross-lane contradiction audit, exact changed-path audit, unchanged
  AInativePM status/SHA proof, `git diff --check`, evidence-backed proposal,
  explicit unresolved product choices, sealed commit, guarded landing, push,
  and teardown.

## Risk and recovery

- Primary risks: designing from the old anchoring hypothesis, treating
  AInativePM as software-only, copying external status into PC-SDK truth,
  mistaking an MCP timeout for failure/success, or performing a remote write
  during discovery.
- Recovery: all external inspection is read-only; ambiguous behavior remains
  unknown; no integration code or migration is created; any accidental state
  change stops the slice and is reported rather than hidden.

## Agent plan

- Domain/persistence lane: AInativePM schema, migrations, generic operations,
  hierarchy, membership, lifecycle, events, and source-of-truth rules.
- UI/REST lane: human workflows and code-level parity across generic views,
  detail surfaces, routes, and failure states.
- MCP/integration lane: AInativePM tools/auth/idempotency/health plus current
  PC-SDK MCP and `pmRef` touchpoints.
- Root owns live read-only browser/MCP evidence, cross-lane synthesis, product
  decision framing, acceptance, and landing. All writes serialize in the
  PC-SDK documentation worktree only.

## Completion receipt

Pending evidence gathering, proposal review, guarded landing, push, and
teardown.
